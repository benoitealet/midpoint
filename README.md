# mJail
mail jail, smtp sandbox for web developers

Prerequisites:
mongodb, yarn

Config:
Edit config/config.json

Start:
in root folder:
$ yarn install
$ node midpoint.js

Start front:
in front-mjail folder:
$ yarn install
$ yarn start
